{
	private ["_txt"];
	_txt = format ["//%1
	_groupList pushBack [", groupID _x];

	{
		_txt = _txt + (format ["
			['%1','%2'],", typeOf _x, rank _x]);
	} forEach units _x;

	_txt = _txt + "
	];";

	diag_log _txt;
} forEach allGroups;

systemChat "Printed Code";